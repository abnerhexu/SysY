int M = 20;
int N = 100;
int arr[20];

int main(){
    int ans;
    int i = 0, j = 0;
    while (i < M) {
        arr[i] = i + 1;
        i = i + 1;
    }
    j = 8;
    arr[0] = arr[arr[2] + arr[j]];
    ans = arr[0];
    return ans;
}